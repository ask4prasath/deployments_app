<%= form_for(@deploy_history) do |f| %>
  <% if @deploy_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@deploy_history.errors.count, "error") %> prohibited this deploy_history from being saved:</h2>

      <ul>
      <% @deploy_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :release %>
    <%= f.text_field :release %>
  </div>
  <div class="field">
    <%= f.label :target_config_name %>
    <%= f.text_field :target_config_name %>
  </div>
  <div class="field">
    <%= f.label :mode %>
    <%= f.select :mode, ["devqa", "ops", "dev"] %>
  </div>
  <div class="field">
    <%= f.label :comments %>
    <%= f.text_area :comments %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
